# secrets — never commit
.env

# telethon session files (account logins)
*.session
*.session-journal

# dedup database
*.db

# python
__pycache__/
*.py[cod]
.venv/
venv/

# konfigurasi hidup di server — jangan pernah ketiban git pull
fleet.json
config.json
